name: Staging Deploy

on:
  push:
    branches:
      - 'claude/**'
    paths:
      - 'angeln-brandenburg/**'
      - '.github/workflows/staging.yml'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  staging:
    name: 🔧 Deploy to Staging
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      # ── 1. Code auschecken ──────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 2. SSH-Key einrichten ────────────────────────────────────
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" | base64 -d > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts 2>/dev/null
          ssh-keygen -l -f ~/.ssh/id_rsa && echo "✅ Key OK" || echo "❌ Key ungültig"

      # ── 3. Dateien per rsync deployen ───────────────────────────
      - name: Deploy to server (Maintenance Mode AN)
        run: |
          DEST="${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/home/${{ secrets.SSH_USER }}/public_html"
          RSYNC="rsync -az --checksum -e 'ssh -p ${{ secrets.SSH_PORT }}'"

          echo "📤 Config-Dateien (Maintenance Mode AN)..."
          rsync -az -e "ssh -p ${{ secrets.SSH_PORT }}" \
            angeln-brandenburg/config/maintenance.html \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/home/${{ secrets.SSH_USER }}/public_html/maintenance.html
          rsync -az -e "ssh -p ${{ secrets.SSH_PORT }}" \
            angeln-brandenburg/config/.htaccess \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/home/${{ secrets.SSH_USER }}/public_html/.htaccess
          rsync -az -e "ssh -p ${{ secrets.SSH_PORT }}" \
            angeln-brandenburg/config/robots.txt.maintenance \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/home/${{ secrets.SSH_USER }}/public_html/robots.txt

          echo "📤 Plugins..."
          rsync -az --delete -e "ssh -p ${{ secrets.SSH_PORT }}" \
            angeln-brandenburg/plugins/angeln-bb-core/ \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/home/${{ secrets.SSH_USER }}/public_html/wp-content/plugins/angeln-bb-core/
          rsync -az --delete -e "ssh -p ${{ secrets.SSH_PORT }}" \
            angeln-brandenburg/plugins/angeln-bb-beissindex/ \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/home/${{ secrets.SSH_USER }}/public_html/wp-content/plugins/angeln-bb-beissindex/
          rsync -az --delete -e "ssh -p ${{ secrets.SSH_PORT }}" \
            angeln-brandenburg/plugins/angeln-bb-pegel/ \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/home/${{ secrets.SSH_USER }}/public_html/wp-content/plugins/angeln-bb-pegel/
          rsync -az --delete -e "ssh -p ${{ secrets.SSH_PORT }}" \
            angeln-brandenburg/plugins/angeln-bb-ai-content/ \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/home/${{ secrets.SSH_USER }}/public_html/wp-content/plugins/angeln-bb-ai-content/

          echo "📤 Theme..."
          rsync -az --delete -e "ssh -p ${{ secrets.SSH_PORT }}" \
            angeln-brandenburg/themes/angeln-bb/ \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/home/${{ secrets.SSH_USER }}/public_html/wp-content/themes/angeln-bb/

          echo "✅ Staging-Deploy abgeschlossen"

      # ── 4. Summary ───────────────────────────────────────────────
      - name: Staging summary
        run: |
          echo ""
          echo "======================================"
          echo "🔧 STAGING bereit zur Prüfung:"
          echo ""
          echo "  https://brandenburg-angler.de/"
          echo "  (Besucher sehen Maintenance-Seite)"
          echo ""
          echo "  WP-Admin Login:"
          echo "  https://brandenburg-angler.de/wp-admin/"
          echo "======================================"
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"
          echo "Zeit:   $(date '+%d.%m.%Y %H:%M:%S')"
          echo "======================================"
